# Optional. Set to route the LLM leg to a real Claude model. Unset -> deterministic MockProvider.
ANTHROPIC_API_KEY=

# Optional. 32-byte hex master key for the encrypted store. Unset -> ephemeral random key.
SCP_MASTER_KEY=
