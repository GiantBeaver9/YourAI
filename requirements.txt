# Core (always required)
cryptography>=41.0
faker>=24.0

# Detection substrate (intended default). A native-regex fallback runs when these are absent,
# so keyless CI still works — see secure_context_pipeline/detection/factory.py.
presidio-analyzer>=2.2
# Presidio needs a spaCy model at runtime:
#   python -m spacy download en_core_web_sm

# HTTP service (deployment entrypoint — see secure_context_pipeline/api.py)
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9   # PDF/file uploads (/process-pdf)
pypdf>=4.0                # PDF text extraction (/process-pdf)

# Real LLM leg (optional — MockProvider is the keyless default).
# Gemini is the intended real provider for this deployment (google-genai SDK).
google-genai>=1.0
anthropic>=0.39

# Test / property-based
pytest>=8.0
pytest-asyncio>=0.23
hypothesis>=6.0
